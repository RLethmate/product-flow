/* Einzelmodus: statisches Deployment auf GitHub Pages.
 * apiBase leer = es wird nichts gesendet, das Ergebnis entsteht nur im Browser.
 * Für Lead-Erfassung apiBase auf das Backend zeigen lassen, z. B.
 *   apiBase: "https://quick-check.it-team-flow.de"
 */
window.QC_CONFIG = {
  mode: "solo",
  apiBase: "",
  askForContact: true
};
